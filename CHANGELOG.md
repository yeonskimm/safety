# 변경 이력 (CHANGELOG)

「오늘의안전」의 버전별 개선 내역입니다. 최신 버전이 위에 있습니다.
반영일은 이 저장소에 해당 버전이 올라간 날짜(커밋 기준)입니다.

---

## v97 — 2026.9.24

**배경** 같은 계정(yeonskimm.github.io)에서 운영하는 다른 앱(「법ON」)과 함께 설치하면, 한쪽이 업데이트될 때 다른 앱의 오프라인 캐시까지 지울 수 있는 구조였습니다.

- 서비스워커의 캐시 정리 범위를 이 앱의 캐시(`onul-safety-` 접두어)로 한정
- 같은 도메인의 다른 앱과 오프라인 캐시가 서로 간섭하지 않도록 개선

---

## v96 — 2026.9.23

**배경** 법령 개정(시행령·시행규칙 2026.8.1 시행) 반영이 필요했고, 기존 법령 DB는 문서 변환 과정에서 일부 조문이 손상되어 검색에서 제외되어 있었습니다.

- **법령 DB 전면 재생성** — 국가법령정보센터 원문 기준으로 4개 법령 1,230개 조문 재구축
  - 2026.8.1 시행 시행령·시행규칙 개정 반영
  - 남아 있던 손상 조문 복구(법 제1조, 산업안전보건기준에 관한 규칙 제2조)
  - 벌칙·과태료 조문 복구(법 제167·168·169·174·175조)
  - 산업안전보건기준에 관한 규칙의 표 내용 복구
- **AI 프록시 보호** — AI 시스템 프롬프트를 서버에 고정해 앱 밖에서 역할 변경 불가, IP당 요청 한도(분당 15회·시간당 120회)·답변 길이 상한 적용
- **개인정보 안내**
  - 의견 보내기에 연락처 수집·이용 동의 추가
  - AI 상담 입력창 위에 "외부 AI로 전송되니 이름·연락처는 입력하지 마세요" 안내
- 답변 하단 법령 근거 칩의 색상 시인성 개선

---

## v95 — 2026.9.20

**배경** 질문 표현에 따라 관련 조문을 찾지 못하는 경우가 있었고, 이용자가 AI 답변의 근거를 확인할 방법이 없었습니다.

- **법령 검색 정확도 개선** — 띄어쓰기 없이 붙여 쓴 질문(예: "도급인안전조치의무는?")도 단어를 나눠 검색
- AI 답변에 **실제로 주입된 조문**을 `📖 법령 근거` 칩으로 표시
- 손상되어 검색에서 제외했던 조문 복구(법 제40·51·63조)
- **근거 없는 법률 답변 차단** — 법령 DB를 불러오지 못하면 법 조항 질문에는 답하지 않음
- 요청 한도 초과 안내 개선 — 분당 한도(1분 후 재시도)와 일일 한도를 구분해 안내
- 질문 입력 길이 상한 적용, AI 답변의 마크다운 제목 기호가 그대로 보이던 문제 수정
- 관리자 화면의 의견 연락처를 기본 가림 처리(탭하면 표시)
- GitHub Actions 자동 검사 도입 — 저장소에 올릴 때마다 전체 검사 자동 실행

---

## v94.1 (핫픽스) — 2026.9.19

**배경** 시행규칙 제73조제2항 관련 답변을 점검하던 중, AI가 조문을 요약하면서 적용 요건·단서("각 호의 모두에 해당하지 않는 사업주", "처음 발생한 재해" 등)를 빠뜨리는 문제를 발견했습니다. 요건이 빠지면 법 적용 대상을 잘못 안내할 수 있어 다음 버전을 기다리지 않고 당일 수정했습니다.

- 조문에서 적용 요건·단서를 따로 뽑아 AI에 별도로 명시(요약 중 누락 방지)
- 해당 사례를 자동 검사 항목에 추가
- 서버(Worker)만 수정, 앱 버전은 v94 유지

---

## v94 — 2026.9.19

**배경** 관리자 비밀번호가 앱 코드 안에 있어 노출 위험이 있었고, AI가 조문 전체가 아닌 일부만 보고 답하거나 존재하지 않는 조문번호를 쓸 가능성이 있었습니다.

- **관리자 인증 서버 이전** — 앱 코드에서 비밀번호·토큰 제거, 서버 검증 후 세션 토큰 발급
- **항(項) 단위 조문 주입** — 질문과 관련된 항을 정확히 골라 AI에 전달
- **답변 조문번호 검증** — AI 답변에 주입하지 않은 조문번호가 있으면 표기를 제거하고 확인 안내
- 법령 DB 정비 — 별표 13종 수록, 공백 정규화, 손상 조문 검색 제외
- AI 요청 한도 예측 라우팅 — 주 모델(Groq) 한도가 바닥나면 실패를 기다리지 않고 예비 모델(Gemini)로 바로 전환
- 법령 DB 빌드·검증 도구(`tools/`)와 배포 전 자동 검사(`tests/`) 도입

---

## v92 — 2026.8.25

**배경** 기존 AI 모델(`llama-3.3-70b-versatile`)의 서비스 종료(2026.8.16)가 예고되었습니다.

- AI 모델을 `openai/gpt-oss-120b`로 교체
- 새 모델의 추론 방식에 맞춰 응답 설정 조정

---

## 이전 버전

- 2026.4.25 저장소 등록. v39~v91 변경 내역은 이 저장소의 커밋 기록을 참고하십시오.
